public package hello
from std.io import printLine

struct Animal {
    name string
    age uint
}

struct Dog {
    extend Animal
    tag string
}

func makeNoise(dog &Dog) {
    printLine(dog.name)
}
