pub package animals

pub struct Animal {
    pub name string
    pub age uint32
}
pub func makeNoise(animal &Animal)

pub struct Dog {
    Animal
    pub tag string
}

pub struct Cat {
    Animal
    pub breed string
}

pub func makeNoise(dog &Dog) {}

pub func makeNoise(cat &Cat) {}

pub func main() {
    var animal mut &Animal = &Dog()
    makeNoise(animal)
    animal = &Cat()
    animal->makeNoise()
}


pub package moreAnimals
from animals import *

pub struct Pet {
    Animal
} cases {
    Dog, Cat, ..
}
pub func walk(pet &Pet)

pub struct Hamster {
    Pet
}

pub func makeNoise(hamster &Hamster) {}

pub func walk(hamster &Hamster) {}

pub func walk(dog &Dog) {}

pub func walk(cat &Cat) {}




pub package shapes
from std.math import pi

pub struct Shape cases {
    struct Circle {
        pub radius float64
    }

    struct Rectangle {
        pub width, height float64
    }

    struct Triangle {
        pub width, height float64
    }
}
pub func area(shape &Shape) float64

pub func area(circle &Circle) float64 {
    return pi*circle.radius*circle.radius
}

pub func area(rectangle &Rectangle) float64 {
    return rectangle.width*rectangle.height
}

pub func area(triangle &Triangle) float64 {
    return 0.5*triangle.width*triangle.height
}

pub func main() {
    var shape mut Shape = Circle(1)
    printLine(shape->area())
    shape = Rectangle(1, 2)
    printLine(area(shape))
    shape = Triangle(1, 2)
    printLine(shape->area())
}




pub package physics
from std.operations import Equality, Arithmetic, To

pub struct Entity {
    pub pos, vel, acc Vector
} cases {
    struct Circle {
        pub radius float64
    }

    struct Mesh {
        pub vertices owned []Vector
    }
}
pub func update(entity &mut Entity)

pub struct Vector {
    Equality, Arithmetic, To[string]
    x, y, z float64
}

pub func collide(a, b &mut Entity) {
    match a, b {
        case &Circle, &Circle:
        case &Mesh, &Mesh:
        case &Circle, &Mesh:
        case &Mesh, &Circle:
    }
}

pub func update(circle &mut Circle) {}

pub func update(mesh &mut Mesh) {}

pub func to[string](vector &Vector) {}




pub package std.collections
from std.traits import *
from std.operations import *

pub struct List[T Sized] {
    Equality, Comparison, TryAirthmetic, To[string], TryFrom[string]
    length usize
    elements []T
}

pub func .length(list &List) usize => list.length

pub func .capacity(list &List) usize => list.elements.length




package chess
from std.io import printLine, inputLine
from std.operations import tryTo, tryFrom

struct Board [8][8]Space

struct Space cases {
    default empty
    struct Piece {
        type PieceType
        color Color
    }
}

struct Color cases {
    black, white
}

struct PieceType cases {
    pawn, bishop, knight, rook, queen, king
}

func new(board &mut Board) {
    board[0] = [0...8 = (.pawn, .black)]
    board[1] = [
        (.rook, .black), (.knight, .black), (.bishop, .black),
        (.queen, .black), (.king, .black),
        (.bishop, .black), (.knight, .black), (.rook, .black),
    ]
    board[6] = [0...8 = (.pawn, .white)]
    board[5] = [
        (.rook, .white), (.knight, .white), (.bishop, .white),
        (.queen, .white), (.king, .white),
        (.bishop, .white), (.knight, .white), (.rook, .white),
    ]
}

func getCoordinate(coordinate string) optional[(usize, usize)] {
    if coordinate.length == 2 and 'a' <= coordinate[0] <= 'h' and '1' <= coordinate[1] <= '8' {
        var column = 8 - ('h' - coordinate[0])
        var row = tryTo[usize](cooridnate[1])->unwrap() - 1
        return (row, column)
    }
    return none
}

func getCoordinates(move string) optional[((usize, usize), (usize, usize))] {
    
}

func getMove(turn uint32) {
    print(f"{turn}. ")
    var move = 
}

func main() {
    // print welcome message
    // whites turn
    // get move
    // check move
    // move piece
    // capture?
    // castle?
    // check?
    // win?
    // blacks turn

    var board mut Board
    var turn mut uint32

    printLine("**** CHESS ****\n")

    while true {
        print(f"TURN {turn/2 + 1}, ")
        if turn % 2 == 0 {
            printLine("BLACK'S MOVE")
        } else {
            printLine("WHITE'S MOVE")
        }

        var move string = getMove()
    }
}




pub struct any[T..] cases {
    T, ..
}

pub struct all[T..] {
    T
}
