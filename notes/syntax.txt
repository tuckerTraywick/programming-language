struct UserId uint32



struct Vector {
    x, y float64
}


struct Direction uint32 cases {
    n, s, e, w
}


struct Color {
    r, g, b uint8
} cases {
    default struct Rgb
    red = (255, 0, 0)
    green = (0, 255, 0)
    blue = (0, 0, 255)
}


struct Animal {
    name string
} cases {
    struct Dog {
        tag string
    }

    struct Cat {
        breed string
    }
}


pub struct AbstractsDog cases {
    Dog, ...
}
pub func Generic[T AbstractsDog](x T)




pub package animals

pub struct Animal {
    pub name string
    pub age uint32
    pub walk(&this)
}

pub struct Dog {
    Animal
    pub tag string
}

pub struct Cat {
    Animal
    pub breed string
}

pub func walk(dog &Dog)

pub func walk(cat &Cat)

pub func main() {
    var animal mut &Animal = &Dog()
    animal.makeNoise()
    animal = &Cat()
    animal.makeNoise()
}


pub package moreAnimals
from animals import *

pub struct Pet {
    Animal
    pub walk(&this)
} cases {
    Dog, Cat, ..
}

pub struct Hamster {
    Pet
}

pub func makeNoise(hamster &Hamster)

pub func walk(hamster &Hamster)

pub func walk(dog &Dog)

pub func walk(cat &Cat)




pub package lexer
from std.io import File->*, FileError->*

pub struct TokenType uint32 cases {
    default invalid
    keyword, identifier, number, string
}

pub struct Token {
    pub type TokenType
    pub text string
}

pub func lex(file File) any[owned []mut Token, FileError]

pub func lex(text string) []mut Token




pub package std.collections
import std.operations

pub struct List[T Sized] {
    from std.operations import *
    Equality, TryComparison, TryArithmetic, TryBitwise, TryLogical
    To[bool], To[string], To[owned []T], From[[]T]

    length usize
    elements owned []mut T
}

pub func append[T](list &mut List[T], value &T)
