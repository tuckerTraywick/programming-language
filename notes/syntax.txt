&int32, []int32
owned &int32, owned []int32
mut owned optional


struct Animal {
    name string
    age uint32
}

union Direction of uint32 {
    n, s, e, w
}

union List {
    struct End
    struct Node {
        next owned &List
        data int32
    }
}

pub func lex(file File) union{owned []Token, FileError}


struct List {
    first owned &Node
    last &Node
}

struct Node {
    next owned &Node
    data int32
}


pub struct Noisy

pub makeNoise(noisy &Noisy)

pub struct Dog {
    Noisy
    pub tag string
}

pub struct Cat {
    Noisy
    pub breed string
}

pub func makeNoise(dog &Dog) {}

pub func makeNoise(cat &Cat) {}
