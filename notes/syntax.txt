// Example of the basic syntax:
package example
import std.io.printLine

var a = 1
var b int = 2
var c string

func foo(x) => x + 1
func bar(x, y int) int {
    return x + y
}

struct Thing {
    a int
    b string
}

func main() {
    printLine("Hello, world!")
}




// Example of struct embedding and methods:
pub package animals
import std.io.printLine

pub struct Animal {
    pub name string
    pub age uint
    pub makeNoise(&this)
}

pub struct Dog {
    embed Animal
    pub tag string
}

pub struct Cat {
    embed Animal
    pub breed string
}

pub func makeNoise(dog &Dog) {
    printLine("woof")
}

pub func makeNoise(cat &Cat) {
    printLine("meow")
}

pub func main() {
    var pet mut &Animal = &Dog()
    pet.makeNoise() // prints "woof"
    pet = &Cat()
    pet.makeNoise() // prints "meow"
}




// Examples of type definitions:
alias MyAlias = int

struct MyStruct = int

struct Point {
    x, y int
}

// Behaves like an enum with the given cases.
struct Direction cases {
    n, s, e, w
}




// Example of a type with both fields and cases:
package colors

// Behaves like an enum with cases that have the given fields. See below for usage.
struct Color {
    r, g, b uint8
} cases {
    default black = (0, 0, 0)
    white = (255, 255, 255)
    red = (255, 0, 0)
    green = (0, 255, 0)
    blue = (0, 0, 255)
}

func main() {
    var color mut Color
    print(color) // prints "Color.black(r=0, g=0, b=0)"
    color = Color.red
    print(color.r) // prints "255"
    color = .blue // shorthand for "Color.blue"
    print(color.g) // prints "0"
}




// Example of a type with cases that are themselves types:
package shapes
import std.math.pi
import std.io.printLine

struct Shape {
    area(this)
} cases {
    struct Circle {radius float64}
    struct Rectangle {width, height float64}
    struct Triangle {base, height float64}
}

func area(c Shape.Circle) => pi*c.radius*c.radius
func area(r Shape.Rectangle) => r.width*r.height
func area(t Shape.Triangle) => 0.5*t.base*t.height

func main() {
    var shape mut Shape = .Circle(2) // Shorthand for "Shape.Circle(2)"
    printLine(shape.area()) // prints "12.566..."
    shape = .Rectangle(2, 5)
    printLine(shape.area()) // prints "10"
    shape = .Trianlge(2, 5) // prints "5"
}




// Examples of function types:
func(int, x string, bool) // unnamed int, int named x, unnamed bool
func(a int, b string) // int named a, string named b
func(a, b int) // int named a, int named b




// Built-in types:
Error None Maybe[T] Any[T..] All[T..]
Owned[T] Shared[T] Weak[T]
