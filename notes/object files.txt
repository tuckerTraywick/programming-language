name -> symbol
symbol -> name
qualified name -> name
name -> qualified name

object file format:
map of module names to maps of symbol names to symbols

the symbol table must be nested
you can't easily deserialize a newsted symbol table because the data is stored inline with the header


modules
variables
functions
strings
data
code
