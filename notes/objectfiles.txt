$ readFile("api.txt")
public package api
public func foo(x int) int => x + 1
$ compile("api.txt"); load("api.obj")
$ api.foo(1)
2
$ trace(api.foo(1))
repl: std.debug.trace(api.foo(1))
    api.txt:4: api.foo(1)
$ watch(api.foo(1)) # enters tui debugger


# IDE could automatically load project into built-in terminal when compiling so you can directly
# test code afer building. IDE could let you set breakpoints and stuff and then immediately run a
# function in the console.
