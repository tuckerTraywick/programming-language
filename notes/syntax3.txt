a := 1;
a: int = 1;

f(x: int) -> int => x + 1;
f(a, b: int) -> int {
    return a + b;
}

struct Animal {
    name: string;
    age: uint;
    makeNoise(&this);
}

struct Dog {
    embed Animal;
    tag: string;
}

struct Cat {
    embed Animal;
    breed: string;
}

makeNoise(dog: &Dog) {
    printLine("woof");
}

makeNoise(cat: Cat) {
    printLine("meow");
}

main() {
    pet: mut &Animal = &Dog();
    pet.makeNoise();
    pet = &Cat();
    pet.makeNoise();
}


